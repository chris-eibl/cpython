name: Windows ARM64

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - '3.*'
  pull_request:
    branches:
      - main
      - '3.*'

permissions:
  contents: read

jobs:
  build-windows:
    name: Windows ARM64
    uses: ./.github/workflows/reusable-windows.yml
    with:
      arch: arm64
      free-threading: false
      interpreter: switch-case
